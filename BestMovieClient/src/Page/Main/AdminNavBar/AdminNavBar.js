const AdminNavBar = () => {
    return (
        <div> admin nav bar
            1) user register + email template ms with code + client time expire code
                1.1 Email template ms with mailer and public contract (another one MS BestMovieCommon + register on packagist)
                1.2 Check confirmation code + time of expire
                1.3 Redis cache - save code to 3 minutes
        </div>
    )
}